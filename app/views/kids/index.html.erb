<h1>Kids in the School:</h1>

<%if logged_in?%>
  <% @kids.each do |k|%>
    <% if k.name.present? %>
      <p><%=k.name.capitalize%>, age: <%=k.age%>, parent: <%=k.parent.name.capitalize%></p>
    <%end%>
  <%end%>
  <%=render partial: 'parents/goback'%>
<%else%>
  <p>You need to be logged in to see this.</p>
<%end%>
